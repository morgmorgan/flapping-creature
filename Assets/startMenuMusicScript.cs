using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[RequireComponent(typeof(AudioSource))]
public class startMenuMusicScript : MonoBehaviour
{
    //public AudioClip music_to_play;
    //public float seconds_between_music = 4f;

    //AudioSource audio_player;
    //settingsScript gameSettings;

    //// Start is called before the first frame update
    //void Start()
    //{
    //    gameSettings.
    //    audio_player = GetComponent<AudioSource>();
    //    StartCoroutine(soundLoop());
    //}

    //IEnumerator soundLoop()
    //{
    //    audio_player.PlayOneShot(music_to_play);
    //    yield return new WaitForSeconds(seconds_between_music);
    //    StartCoroutine(soundLoop());
    //}
}
